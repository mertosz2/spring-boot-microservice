eureka:
  instance:
    hostname: localhost
  client:
      register-with-eureka: false
      fetch-registry: false
      serviceUri:
        defaultZone: http://localhost:8761/eureka


server:
  port: 8761
